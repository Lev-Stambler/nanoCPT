# Legacy Records

Retired leaderboard and experiment records live here so the active `records/`
tree is reserved for the current v3 hard typo-noised corpus.

- `track_2_5min/v1`: leaky packed-attention records on the clean conlang corpus.
- `track_2_5min/v2`: doc-aware packed-attention records on the clean conlang corpus.

These artifacts remain for reproducibility only. New accepted records should be
written under `records/<track>/v3/`.
